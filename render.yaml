services:
  - type: web
    name: margin-app
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: margin-app-db
          property: connectionString

databases:
  - name: margin-app-db
    plan: free
